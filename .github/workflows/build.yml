name: Build

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Detect Go modules
        id: readiness
        shell: bash
        run: |
          if find . -path './.git' -prune -o -name go.mod -type f -print -quit | grep -q .; then
            echo "active=true" >> "$GITHUB_OUTPUT"
          else
            echo "active=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Set up Go
        if: steps.readiness.outputs.active == 'true'
        uses: actions/setup-go@v7
        with:
          go-version: stable
      - name: Build Go modules
        if: steps.readiness.outputs.active == 'true'
        shell: bash
        run: |
          while IFS= read -r module; do
            module_dir="$(dirname "$module")"
            echo "Building $module_dir"
            (cd "$module_dir" && go build ./...)
          done < <(find . -path './.git' -prune -o -name go.mod -type f -print)
      - name: Explain inactive stage
        if: steps.readiness.outputs.active != 'true'
        run: |
          echo "Build stage is inactive: no Go modules are present in the bootstrap repository."
